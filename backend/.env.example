# ===========================================
# Django Backend Environment Configuration
# ===========================================
# IMPORTANT: Copy this file to .env and fill in your values
# Do NOT commit .env to version control!

# ===========================================
# SECURITY - CRITICAL FOR PRODUCTION
# ===========================================
# Generate a secure secret key using: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=fpV-fUxYMB2ee4y2PGHdQPzhZSBNPie8K121qJeVId16MccChiy4r_yMF33B_15i7q8

# Set to False in production!
DEBUG=True

# Comma-separated list of allowed hosts (use * for development only)
ALLOWED_HOSTS=localhost,127.0.0.1,10.206.54.107

# ===========================================
# Database Configuration
# ===========================================
# For development (SQLite - default):
# DATABASE_URL=sqlite:///db.sqlite3

# For production (PostgreSQL recommended):
# DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# ===========================================
# Production Security Settings (uncomment for production)
# ===========================================
# SECURE_HSTS_SECONDS=31536000  # 1 year
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True

# ===========================================
# CORS Configuration
# ===========================================
# Development: Allow Expo Go and localhost
# CORS_ALLOWED_ORIGINS=http://localhost:8081,http://127.0.0.1:8081

# Production: Replace with your mobile app's domain
# CORS_ALLOWED_ORIGINS=https://yourapp.expo.dev,https://yourdomain.com

# ===========================================
# JWT Settings
# ===========================================
JWT_ALGORITHM=HS256
ACCESS_TOKEN_LIFETIME=900       # 15 minutes in seconds
REFRESH_TOKEN_LIFETIME=604800   # 7 days in seconds

# ===========================================
# Email Configuration (for password reset)
# ===========================================
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# ===========================================
# Optional: Logging Configuration
# ===========================================
# LOG_LEVEL=DEBUG
